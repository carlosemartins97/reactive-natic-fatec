import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginRight: 10,
    backgroundColor: '#ddd',
    minWidth: 150,
    minHeight: 150,
    maxWidth: 150,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 8,
    padding: 8,
  },
  info: {
    marginTop: 12,
    fontWeight: 'bold'
  }
});
